<div class="c-input-password{{#if className}} {{ className }}{{/if}}">
  {{> Input type="password" className="c-input_password" label=label id=id name=name value=value required=required }}
  <div class="c-input-password__validation c-input-validation">
    {{ textValid }}
  </div>
</div>
